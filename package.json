{
  "name": "approve-workflow-to-run",
  "version": "1.0.0",
  "description": "A custom GitHub Action that runs Node.js to approve workflows",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "github-action",
    "workflow",
    "approval",
    "node"
  ],
  "author": "WakuwakuP",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
